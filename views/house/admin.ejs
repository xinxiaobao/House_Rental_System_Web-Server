<table class="table">

    <% houses.forEach( function(model) { %>
   
    <tr>
        <th><%= model.id %></th>
        <th><%= model.title %></th>
        <th><%= model.name%></th>
        <th><a href="/house/edit/7">edit</a></th>
    </tr>

    <% }); %>

</table>