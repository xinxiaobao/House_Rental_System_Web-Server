<table class="table">

    <% houses.forEach( function(model) { %>
   
    <tr>
        <th><%= model.id %></th>
        <th><%= model.title %></th>
        <th><%= model.name%></th>
        <th><a href="/house/edit/<%= model.id %>">edit</a></th>
    </tr>

    <% }); %>

</table>